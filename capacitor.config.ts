import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Asistencia_def',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
